import Login from './components/UserAccount/login/login';
function App() {
  return (
    <div className="App">
       {/* //! added by abed, it's just to test the component --  you can remove it before the finish. */}
       <Login />
    </div>
  );
}

export default App;
